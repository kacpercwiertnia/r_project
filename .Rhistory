dunif(x, min = 0, max = 1000)
x <- runif(10, 0, 1000)
x
x <- runif(10, 0, 10)
x
x <- runif(100, 0, 10)
x
install.packages("smoof")
print(f(point))
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
f <- makeAlpine01Function(dim);
library(smoof)
makeAlpine01Function()
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm(makeAlpine01Function, 2, 50, -10, 10)
f <- makeAlpine01Function(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- makeAlpine01Function(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(1, 2, 50, -10, 10)
View(prs_algorithm)
View(prs_algorithm)
View(prs_algorithm)
View(prs_algorithm)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function, 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function, 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function, 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
prs_algorithm <- function(fun, dim, num_of_points, min_range, max_range){
f <- fun(dim);
for(x in 1:num_of_points){
point <- runif(dim, min_range, max_range);
print(f(point))
}
}
prs_algorithm(makeAlpine01Function(), 2, 50, -10, 10)
library(smoof)
import::here(makeAlpine01Function, .from = smoof)
import::here(makeAlpine01Function, .from = smoof)
install.packages("smoof")
library(smoof)
capabilities("tcltk")
library(smoof)
